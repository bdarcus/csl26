# Intentional Divergence Register

This document records intentional behavioral departures from legacy CSL/citeproc where Citum has chosen a different rendering strategy based on publisher rules, biblatex prior art, or design principles.

## Purpose

The Divergence Register serves as a **durable audit trail** of known structural and formatting differences between Citum output and citeproc-js for a given style. It allows style-wave authors and maintainers to:

1. Distinguish intentional divergences from defects during oracle review
2. Track the authority basis for each choice (publisher rules, biblatex precedent, design principle, or documentary evidence)
3. Communicate expectations to downstream users and tooling integrations
4. Version divergence decisions as styles evolve across phases

## Extension Protocol

When adding a new intentional divergence:

1. **Assign the next sequential ID**: Use `div-NNN` where NNN is the next integer after the highest existing ID.
2. **Classify the authority basis**: Select one of four values:
   - `Publisher rules` — The behavior matches how the authoritative publisher (Nature, ACS, IEEE) handles that element
   - `Biblatex prior art` — The behavior mirrors biblatex's well-established convention
   - `Citum design principle` — The behavior follows an explicit Citum architectural decision (e.g., explicit-only policies)
   - `Documentary evidence` — The behavior is grounded in a published standard, specification, or style guide
3. **Note oracle/compat impact**: State whether existing oracle/compatibility snapshots should be updated or left as known mismatches
4. **Reference context**: Optionally link to the relevant bean, commit, or discussion that motivated the divergence

## Divergence Table

| ID | Styles | Behavior | Legacy CSL/citeproc | Citum | Authority | Oracle/Compat Impact |
|----|--------|----------|----------------------|--------|-----------|----------------------|
| div-001 | `nature`, `cell`, `plos`, `acs-nano`, and other numeric-compound styles | Bibliography entries grouped as publisher-level sets (e.g. "1. AuthorA … 2. AuthorB …") rather than rendered as individual references with per-reference group-key logic | Renders each reference independently using `group-by` keys; compound grouping not a native CSL feature | Engine renders compound sets as first-class bibliography units via `compound: true` option | Publisher rules | Causes structural mismatches in oracle output for all compound-numeric styles; expected and accepted |
| div-002 | `angewandte-chemie`, `american-chemical-society`, `rsc`, `numeric-comp` | Format decisions (punctuation, name order, volume/pages delimiter) follow biblatex defaults rather than citeproc output where they conflict | Treated citeproc output as normative reference | Biblatex prior art takes precedence when CSL output appears bibliographically wrong or underspecified | Biblatex prior art | Minor per-field mismatches in oracle comparisons; classified as `legacy-limitation`, not defects |
| div-003 | All author-date, note, and numeric styles | Processing families supply explicit bibliography sort defaults: `author-date` → `author-date-title`; `note` → `author-title-date`; `label` → `author-date-title`; `numeric` → none (insertion order preserved). Explicit style-level `bibliography.sort` always wins. | Applies uniform global bibliography sort; numeric styles may sort alphabetically by default | Each processing family has an explicit sort default; numeric styles preserve insertion order | Biblatex prior art | Minimal impact; most styles set explicit sorts. Insertion-order stability for numeric styles is intentional. |
| div-004 | All styles using author-based sorting | Works with no author/editor/translator sort by title rather than being treated as empty/equal | Behavior underspecified; often sorts missing-name works together at top or bottom inconsistently | When author sort key is absent, falls back unconditionally to title as sort key (removed `author_fallback_to_title` flag) | Biblatex prior art | May cause ordering differences for reference sets with anonymous works; consistent with bibliographic expectation |
| div-005 | All styles | Citation-list sorting never implied by processing family; only explicit `citation.sort` triggers sorting | Some styles rely on implicit citation-list sorting derived from processing class | Phase 1 policy: `citation.sort` explicit-only; absent → preserve input order. Family-level citation sort deferred to later phase. | Citum design principle | Possible ordering differences in citation lists for styles expecting family-implied sort; acceptable Phase 1 limitation |
| div-006 | ~~`new-harts-rules-notes`, `new-harts-rules-notes-label-page`, `new-harts-rules-notes-no-url`~~ | ~~Patent bibliography author name-order: first author inverted (family-first), subsequent authors given-first~~ | ~~All authors inverted when `name-order: family-first` is used in contributor config~~ | **Resolved** — added `NameOrder::FamilyFirstOnly` variant; affected styles updated to `name-order: family-first-only` | Engine fix | Fixed in csl26-zzun |
| div-007 | All styles using `interviewer` contributor with `form: verb` | Verb form renders as `"interviewed by"` | `"interview by"` in upstream `locales-en-US.xml` (missing past participle — grammatically incorrect passive construction) | `"interviewed by"` — standard English past-participle passive for the role | Citum design principle | No oracle impact; no current style uses `form: verb` on interviewer contributor blocks |
| div-008 | All styles using author-based sort with multiple authors sharing a family name | Secondary sort key after author family name: citeproc-js uses given name (family → given → suffix → title); Citum uses title (family → title) | citeproc-js sorts within same-family-name groups by given name ascending | Citum uses title as the secondary sort key after family name — consistent with the title-fallback philosophy of div-003/div-004 and with biblatex `sortname` field semantics | Citum design principle | Causes reordering within same-family-name groups in any author-sorted bibliography; numeric citation labels shift accordingly. Divergence-aware oracle adjustment fires independently alongside div-004 when both conditions are present in the same fixture. |
| div-009 | All author-date styles using `year-suffix` disambiguation with reprinted works | Year-suffix letter keys on the `issued` (reprint) year only; original-publication date is rendered but never enters the collision key. Three reprints (orig 1926/1926/1927, all issued 1967) all receive suffixes: `(1926/1967a) (1926/1967b) (1927/1967c)`. | citeproc-js keys suffix assignment on the full rendered date string; in this scenario the work with a distinct original date does not receive a suffix: `(1926/1967a) (1926/1967b) (1927/1967)`. | Citum keys on `issued` year exclusively (`build_group_key` in `disambiguation.rs`). Working assumption: major author–date systems (APA, Chicago) treat the year of the edition consulted as the operative year for disambiguation; no major style is known to require a different rule. The citeproc-js behavior is undocumented and has no evidence of user dependence. | Documentary evidence | Oracle will show `(1927/1967c)` for Citum vs `(1927/1967)` for citeproc-js in affected reprint scenarios; expected and accepted. |
| div-010 | `chicago-author-date-18th`, `chicago-shortened-notes-bibliography`, `taylor-and-francis-chicago-author-date` (`anonymous-entries: notes-only`); `american-sociological-association` (`anonymous-entries: container-led`) | Bibliography entries for `entry-dictionary`/`entry-encyclopedia`/dictionary-shaped `chapter` references with no visible author are rewritten to lead with the container title; under `notes-only`, entries with no DOI/URL (print-like) are additionally dropped from the bibliography entirely | citeproc-js renders these entries using the style's normal template order and never suppresses them | Behavior is now the declared bibliography option `bibliography.options.anonymous-entries` (`container-led` \| `notes-only`), default unset (off). Chicago's convention is that well-known reference works (dictionaries, encyclopedias) are cited in notes only and omitted from the bibliography unless they have online access; other styles opt into the container-led reordering alone without the suppression. Previously the engine imposed this unconditionally on every style with no way to see or disable it (audit finding 2, `2026-07-04_CITUM_ENGINE_REVIEW_PART2.md`). | Citum design principle | No oracle/compat impact — the four affected styles declare the option that reproduces their prior rendered output exactly (verified byte-identical fidelity scores before/after). Any other style with anonymous reference-work entries now renders them in normal template order instead of being silently rewritten or dropped. |
| div-011 | None yet (no embedded style currently sets `title-quote: by-category`) | An authorless reference cited by title in citation context (the `title` author-substitute key) is quoted unconditionally by default, regardless of reference type | citeproc-js resolves title formatting (quote vs. italic) per reference-category rules, the same as any other rendered title | Citum's default (`contributors.substitute.title-quote` unset) preserves the historical unconditional-quote behavior; the `by-category` option lets a style defer to its `titles:` category rendering instead, so e.g. a book title italicizes rather than quotes. **2026-08-14** (bean `csl26-0dca`): `by-category` was previously half-implemented — it un-quoted the title but the engine's substitute path never applied the category's `emph`/`strong`/`small-caps`, so an italic-book-title style would render neither quotes nor italics. `resolve_title_substitute()` now applies category emphasis whenever the title isn't being quoted (bibliography always; citation only under `by-category` with no `quote: true` on that category), completing the "italicize instead of quoting" contract. | Citum design principle (backward compatibility) | No oracle/compat impact on the historical default (unset `title-quote` stays byte-identical — emphasis is skipped whenever quoting applies). Bibliography entries for author-less title-fallthrough references now pick up category emphasis by default (e.g. `apa-7th` monograph/periodical titles); `chicago-author-date-18th`'s `parent-serial` citation substitute also gains emphasis since that path is never quoted. See PR for `report-core.js` before/after diff. **2026-08-15** (bean `csl26-p7a8`): `docs/specs/SUBSTITUTED_VALUE_FORMATTING.md` evaluates flipping `by-category` per embedded style, broadened to cover every substituted-value kind (title, parent-serial, contributor roles), not just title. Corpus survey of 2844 legacy CSL styles: 94.6% of substituted-contributor `<names>` elements inherit slot formatting (Citum's contributor default already matches the norm, no change recommended); only 2% of styles that reach a title in citation-context `<substitute>` quote it unconditionally (Citum's default is the corpus outlier, confirming `by-category` is the right correction, just not yet opted into). `parent-serial` substitutes are already unconditionally by-category since this fix (never gated by `title_quote`). Oracle-verified (citeproc-js vs. Citum) that flipping `apa-7th` is directionally correct but that every candidate style flip must pair with full `titles.default`/`type_mapping` coverage — `get_title_category_title_rendering`'s hardcoded fallback table recognizes only a narrow type set, so an unconfigured type under `by-category` silently renders neither quoted nor emphasized, a real regression the corpus-survey and `.csl`-source-reading passes alone did not catch. **2026-08-15 (revision)**: `titles.default`/`type_mapping` coverage is necessary but not sufficient for `apa-7th` — its real rule for the residual bucket is "quote if `container-title` is present on *this instance*, else italicize," which the category resolver structurally cannot see (type + config only, no instance data); no type-only mapping can reproduce it. **2026-08-15 (v1.2)**: Resolved — the spec decides against a residual divergence: add a typed `TitleQuoteCondition::ContainerTitlePresent` (`quote-when: container-title-present`) to `TitleRendering`, applied consistently by normal title rendering (`effective_title_quote_depth`) and category-based substitute rendering (`resolve_category_quote`), with precedence explicit `quote` > `quote-when` > no quote. `apa-7th`'s flip is now sequenced last in stage 2 (after `elsevier-harvard`, the T&F CSE style, MLA, and Chicago author-date), gated on this engine capability shipping first — not on Bruce adjudicating an accepted divergence. |
| div-012 | Any style with a `Long`-form contributor for one of `editor`/`chair`/`translator`/`interviewer`/`director`/`illustrator`/`composer` and no `label:`/`role` config (e.g. `elsevier-with-titles-core.yaml`'s editor-in-container components) | With no configuration at all, these seven roles get an engine-hardcoded `" (ed.)"`-style short-form suffix in `Long` form, applied identically whether the component sits in a citation or a bibliography template (`resolve_role_labels` in `labels.rs` never reads `RenderContext`); there is no *global* schema switch to disable this class of default, but a documented per-role escape hatch (`contributors.role.roles.<role>.preset: none`) already suppresses it | citeproc-js has no equivalent implicit default — role labels only render when a style's macro explicitly requests a term. Every style guide examined (APA, MLA, Chicago, Vancouver/NLM) treats role-clarifying labels as a **bibliography/reference-list-only** convention: APA appends `(Ed.)`/`(Eds.)` to an editor standing in for a missing author only in the reference entry, and explicitly does *not* repeat it in the in-text citation (surname + year only); MLA requires a descriptive word-form label ("editor", "director", …) on a broader set of substituted roles, but again only in the Works Cited "author" element, not in the in-text parenthetical/name; Chicago and Vancouver/NLM express roles via phrasing ("edited by …") in notes/reference entries and have no separate in-text role-labeling convention at all. No style examined carries a role label into citation/in-text context under any circumstance. | Citum applies one hardcoded default (shaped like APA's editor convention) to all seven roles, with no context-awareness at all — the same auto-label fires in a citation template as in a bibliography template, which doesn't match *any* examined style's actual behavior (all of them are bibliography-only for this). Separately, only `editor` has real per-role backing even in bibliography context (APA); the other six share the same treatment with no confirmed style-guide justification. Kept only because real embedded styles (e.g. `elsevier-with-titles-core.yaml`) depend on the current bibliography-context default with zero configuration — this is a legacy default carried forward for backward compatibility, not a deliberate cross-style design choice, and the per-role `preset: none` escape hatch is the only way to opt out today. | **Resolved 2026-07-07** (bean `csl26-xve4`): the engine-hardcoded default was removed entirely. Automatic role labels now require an explicit per-style `contributors.role.defaults` bundle (`none`/`apa`/`mla`, default `none`), applied only in bibliography context and never for verb/verb-short forms; a before/after fidelity diff verified no embedded style required compensating configuration (zero regressions; two Chicago-family styles each gained a bibliography pass). See `docs/specs/ROLE_LABEL_DEFAULTS.md`. | Resolved — explicit per-style declaration, bibliography-only | Zero rendered-output change verified via before/after `scripts/report-core.js` diff; audit finding 16b, part 2 engine review |
| div-013 | `apa-7th`; `chicago-author-date-18th` bibliography and descendants | Two-name lists follow `delimiter-precedes-last` literally by default. Styles may declare `contributors.two-name-delimiter-policy: suppress-in-citation-or-given-first` globally or in a citation/bibliography scope to omit the delimiter in those contexts (e.g. "F. A. Editor & S. Editor", "Irino & Tada, 2009"). | CSL's `delimiter-precedes-last` is a plain name-count rule with no name-order or context exception; `always` adds the delimiter for any 2+ names. APA documents an inverted/non-inverted asymmetry, while Chicago oracle evidence likewise omits the comma in given-first secondary-role lists. | **Resolved 2026-08-12** (bean `csl26-fqz2`): literal rule semantics are now the engine default, and the contextual exception is an explicit scoped `ContributorConfig` policy. APA declares it globally; Chicago author-date declares it in bibliography options. CSL migration does not infer the Citum-specific policy. | Documentary and oracle evidence | Resolved — full portfolio fidelity and exact-parity summaries are unchanged before/after; focused tests cover both policies, all delimiter rules, and scoped inheritance. See `docs/specs/TWO_NAME_DELIMITER_POLICY.md`. |
| div-014 | `gb-t-7714-2025-base`, `gb-t-7714-2025-author-date`, `gb-t-7714-2025-note` | The area/terminal `period` mark and the `[J]`/`[M]`-style type-code brackets are authored as literal ASCII (`.`, `[`/`]`) rather than converted to the semantic `period`/`brackets` marks introduced by this PR, even though comma/colon/parentheses in the same templates were converted; the `brackets` mark is additionally pinned back to ASCII via a `scripts.cjk.realization` override so `wrap: parentheses`/`wrap: brackets` on the issue-number and volume components stay correctly full-width while type-code brackets stay ASCII | citeproc-js renders the GB/T area/terminal period and type-code brackets ASCII on Chinese-script entries — the same as on Western entries — never full-width `。`/`【】` | GB/T 7714-2015 §7.2 (「著录用符号」) specifies the period and brackets as ASCII in every example given, for Chinese and Western references alike; unlike comma/colon/parentheses (which real-world/biblatex practice and the citeproc-js oracle render full-width for Chinese entries), the standard and the oracle agree the period and brackets never vary by script. Converting them to the semantic `period`/`brackets` marks would realize `。`/`【】` on Chinese-script items under this style's `realization-default: cjk`, contradicting both sources. | Documentary evidence (GB/T 7714-2015 §7.2) | None — leaving these literal already matches oracle output on all GB/T fixtures (67 en-US + ja-JP/ru-RU/pt-PT and 128 zh-CN items); no snapshot changes needed. Open follow-up (not a divergence): domain-expert review (PR #1073, comment `5032704432`) confirms the punctuation-width axis is a closed four-preset space (`half`/`full`/`mixed`/`bylan`, per biblatex-gb7714-2025 and gbt7714-bibtex-style) and that `mixed` — full-width except period, slashes, dashes, and square brackets — is the citeproc-js oracle's actual behavior, not `bylan` (Citum's current default). Target default (keep `bylan` vs. switch to oracle-faithful `mixed`) and the script-independent realization mode `mixed`/`full`/`half` require remain undecided — see `csl26-xnu9` and `docs/specs/PUNCTUATION_REALIZATION.md` §8. |
| div-015 | `gb-t-7714-2025-author-date`, `gb-t-7714-2025-numeric`, `gb-t-7714-2025-note` | Bibliography entries for a date carrying an opaque calendar `note` (source-calendar wording alongside the canonical Gregorian `value`, e.g. `1947（民国三十六年）` for a Minguo-era publication year) render the wrapped annotation, appended after the complete formatted date. Five records in the pinned corpus exercise this: `gbt7714.7.5.4.1:1`/`:2` (§7.5.4.1 Minguo/Kangxi examples, `1947`/`1705`), `gbt7714.8.2.2:2` (Qing Tongzhi era, `1865`), `gbt7714.8.12.3:1` (Guangxu era, `1887`), `gbt7714.8.12.3:3` (Republic era, `1949`) | citeproc-js / the source CSL-M style render only the bare Gregorian year for all five records — the source implementation has no calendar-annotation mechanism at all, structured or otherwise | Citum implements an opaque `date.note` field (`docs/specs/CALENDAR_DATE_ANNOTATIONS.md`) and a bibliography-scoped `note-wrap` style opt-in; the wrapped note appends after the complete formatted date via the existing script-aware `realize_wrap`, reusing the same full-width/half-width delimiter logic as div-014's `wrap: parentheses`/`wrap: brackets` handling | Documentary evidence (GB/T 7714—2025 §7.5.4.1) | Oracle bibliography comparison for these five records shows Citum's annotated form vs citeproc-js's bare Gregorian year; expected and accepted, not a fidelity regression. `gb-t-7714-2025-numeric` is not in `core-quality-baseline.json`'s gated style set, so no CI fidelity-gate impact; `gb-t-7714-2025-author-date`/`-note` bibliography scope is diagnostic-only (`count_toward_fidelity: false`) regardless. Regression-guarded by a Rust integration test against the real pinned corpus, `crates/citum-engine/tests/date_annotations.rs`. |
| div-016 | Missing `issued` dates | **Resolved 2026-08-16:** missing dates are now blank by default, matching CSL. A style that explicitly authors no-date or alternative-date behavior declares it in `options.date-fallback`; template fallback chains and `dates.no-date-form` were removed. | Real CSL renders a bare missing date silently and renders a no-date term only when the style authors that branch. | [`DATE_FALLBACK.md`](../specs/DATE_FALLBACK.md) centralizes the policy with explicit presets, first/later issued lanes, selector-level `none`, and shared rendering/disambiguation resolution. | Documentary evidence + citeproc-js oracle (`csl26-7z59`) | Resolved — all tracked styles were audited; explicit policies were added only where their source CSL authored fallback behavior. |
| div-017 | `chicago-author-date-18th` and other Chicago-derived author-date heads; any author-date style declaring `citation.collapse: same-author` (see [`SAME_AUTHOR_COLLAPSE.md`](../specs/SAME_AUTHOR_COLLAPSE.md) — the setting that makes same-author collapse itself opt-in; this row only applies where a style has opted in) | Same-author collapse with **no locator** on any item joins the repeated years with a comma: `(Garcia 2019b, 2019a)`. (The **locator-present** half of this same rule is not a divergence — `csl26-uctc` fixed Citum to escalate to semicolon there, matching both CMOS and citeproc-js: `(Garcia 2019b; 2019a, 257)`.) | citeproc-js emits a semicolon even with no locator: `(Garcia 2019b; 2019a)`. Traced to `chicago-author-date.csl`'s `<layout delimiter="; ">` — citeproc-js's `cite-group-delimiter` (undocumented in CSL 1.0.2, an implementation detail) defaults to the layout delimiter, so `"; "` leaks into the within-group join even though CMOS's own rule calls for a comma there. Confirmed against `tests/csl-test-suite/processor-tests/machines/collapse_ChicagoAfterCollapse.json`, whose *other* clause (`<layout delimiter=", ">`) reproduces the comma correctly in the same engine — proof the semicolon is an artifact of Chicago's specific layout delimiter, not a considered CMOS-following choice. | Citum joins with `, ` per CMOS 15.30 (per the CMOS Q&A): "there's a risk that the additional years might be misread as page numbers... but your first approach is preferred for its brevity" — i.e. comma unless a locator is present, in which case switch to semicolon (implemented by `csl26-uctc`). Citum's within-group join (`intra_delimiter`/`group_delimiter`) is architecturally independent of the between-author-group join (`multi_cite_delimiter`), so it does not inherit citeproc's coupling to the layout delimiter. | Documentary evidence (CMOS 15.30) | Pre-existing mismatch, unaffected by `csl26-uctc`'s fix — that fix only changes the locator-present half, moving it *toward* the oracle. Exercised by `disambiguate-year-suffix` and `subsequent-author-consecutive` in `tests/fixtures/citations-expanded.json` (both no-locator same-author-same-year clusters); regression-guarded by `test_chicago_author_date_same_author_collapse_without_locator_stays_comma_joined` in `crates/citum-engine/tests/domain_fixtures.rs` and the CSL-suite replay `test_same_author_collapse_matches_csl_suite_chicago_after_collapse` in `crates/citum-engine/src/processor/tests.rs`. |

## Revision History

- **2026-08-18** (spec, not yet implemented): `docs/specs/SAME_AUTHOR_COLLAPSE.md`
  adjudicates `csl26-ecfn` — same-author collapse becomes opt-in
  (`citation.collapse: same-author`), resolving `csl26-m11m` as a consequence:
  `chicago-notes-18th` and its siblings simply don't declare `collapse` (their
  source CSL doesn't either), which is a property of those specific styles'
  full-sentence-per-citation notes, not of the note regime generally — real
  CSL note styles with short author-year footnotes do declare `collapse` and
  remain fully supported. div-017 is unaffected: `chicago-author-date.csl`
  declares `collapse="year"`, so `chicago-author-date-18th` still opts in and
  the row above still applies.
- **2026-08-18**: Added div-017 — same-author collapse without a locator joins
  with a comma (CMOS 15.30-correct) where citeproc-js emits a semicolon
  (an artifact of Chicago's `<layout delimiter="; ">` leaking into
  `cite-group-delimiter`'s default, not a considered CMOS-following choice).
  Filed alongside `csl26-uctc`, which fixes the companion locator-present half
  of the same rule (previously an outright bug: Citum joined with a comma
  there too, contradicting both CMOS and citeproc-js).
- **2026-08-16**: Resolved div-016 by making missing dates blank and moving
  authored no-date/alternative-date behavior to explicit `date-fallback`
  policy.
- **2026-03-07**: Initial register created with five foundational divergences (div-001 through div-005)
- **2026-03-28**: Added div-006 for Hart's Rules patent author name-order gap
- **2026-04-11**: Resolved div-006 — added `NameOrder::FamilyFirstOnly`; updated 7 chicago/hart's styles
- **2026-03-28**: Added div-007 for `interviewer` verb-form upstream typo (`"interview by"` → `"interviewed by"`)
- **2026-04-12**: Added div-008 for secondary sort key divergence (citeproc-js: given name; Citum: title) after shared family-name match
- **2026-07-04**: Added div-010 — gated the anonymous-entry bibliography policy behind `bibliography.options.anonymous-entries` (audit finding 2, part 2 engine review)
- **2026-07-06**: Added div-011 (substitute-title category-based quoting, gated), div-012 (documented existing per-role escape hatch for the 7-role Long-form label default), and div-013 (two-name delimiter suppression in citation context and given-first bibliography lists, confirmed load-bearing for `apa-7th`) — audit findings 9/16a/16b, part 2 engine review, bean csl26-mc0c
- **2026-07-06**: Corrected div-012 and div-013's authority framing — style-guide research showed neither is a universal cross-style rule (only `editor` has confirmed APA backing for div-012; div-013's inverted/non-inverted split is APA-specific, not universal). Reclassified both from "Citum design principle" to explicit unconfirmed-legacy-default language, and filed follow-up beans `csl26-xve4` (per-style role-label defaults) and `csl26-fqz2` (per-component `delimiter-precedes-last` override) to track the real fixes
- **2026-07-07**: Resolved div-012 — removed the engine-hardcoded 7-role auto-suffix; role labels are now opt-in via `contributors.role.defaults` bundles (`apa`/`mla`), bibliography-context only. Embedded styles relying on the old implicit default were given explicit configuration in the same change (bean `csl26-xve4`, `docs/specs/ROLE_LABEL_DEFAULTS.md`)
- **2026-07-20**: Added div-014 — GB/T 7714 area/terminal period and type-code brackets stay literal ASCII under the semantic-punctuation migration (PR #1073), per GB/T 7714-2015 §7.2 and confirmed against the citeproc-js oracle; the comma/colon/parentheses width question for Western-script entries within a GB/T bibliography remains open pending domain-expert review, tracked as `csl26-xnu9`
- **2026-07-20**: Added div-015 — GB/T 7714 bibliography calendar-date annotations (`docs/specs/CALENDAR_DATE_ANNOTATIONS.md`) render for five real pinned-corpus records across all three GB/T styles; citeproc-js has no equivalent mechanism. Confirmed against the real corpus (`tests/fixtures/test-items-library/gb-t-7714-2025.json`), not synthetic data; regression-guarded by `crates/citum-engine/tests/date_annotations.rs` — PR #1068
- **2026-07-21**: Updated div-014's follow-up with domain-expert findings (PR #1073, comment `5032704432`): the punctuation-width axis is a closed four-preset space (`half`/`full`/`mixed`/`bylan`); `mixed` matches the citeproc-js oracle and means full-width except period, slashes, dashes, and square brackets.
- **2026-07-22**: `csl26-xnu9` implemented the presets and selected `mixed` for embedded GB/T styles; `bylan` remains available for script-conditional output.
- **2026-08-12**: Resolved div-013 with scoped `contributors.two-name-delimiter-policy`; literal `delimiter-precedes-last` semantics are now the default, while APA and Chicago author-date declare their evidenced two-name exceptions explicitly (bean `csl26-fqz2`).
- **2026-08-15**: Updated div-011 with `docs/specs/SUBSTITUTED_VALUE_FORMATTING.md` — a corpus survey and oracle-verified per-style evaluation of flipping `title-quote: by-category`, broadened to cover contributor and parent-serial substitutes alongside title (bean `csl26-p7a8`).
- **2026-08-15** (revision, responding to review): flagged that `titles.default`/`type_mapping` coverage alone can't reproduce `apa-7th`'s real container-title-conditional quoting rule — an instance-level distinction the category resolver can't see. Unresolved, tracked as spec open question 0.
- **2026-08-15** (v1.2): Resolved — the spec chooses to add a typed instance-conditional `quote-when` capability rather than accept a residual divergence; `apa-7th`'s flip is sequenced last in stage 2, gated on that engine work, not on adjudication.
- **2026-08-16**: Added div-016 after documenting the legacy implicit no-date
  default, then resolved it through the explicit fallback-policy contract.
